--@see saker.supervisor

---	local input = {}
--- input['cmd'] = '/opt/app'
--- input['args'] = {'-c=/etc/app.conf', '-s=1'}
--- input['pidfile'] = '/var/run/app.pid'
--- pid, err = saker.adopt(input) 
--- if pid == nil then
---     print(err)
--- end